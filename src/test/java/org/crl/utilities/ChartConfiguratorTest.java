package org.crl.utilities;

import static org.junit.Assert.*;

import org.crl.charts.ChartCropper;
import org.crl.imagedata.Color;
import org.crl.imagedata.Image;
import org.crl.imagedata.StandardColors;
import org.junit.Test;

public class ChartConfiguratorTest {
/*
	@Test
	public void testLineChartCommonConfigurationExtraction() {
		Image imageSampleLineChart = new Image(
				"src/main/resources/TestCharts/LineCharts/Uncut/Arisk_trend.png");
		ChartCropper cropper = new ChartCropper(imageSampleLineChart);
		ChartConfigurator commonConfig=new ChartConfigurator(cropper.cropAxises());
		Color actualColor = commonConfig.getBackgroundColor();
		int actualLineWidth = commonConfig.getMainLineWidth();
		Color expectedColor = StandardColors.WHITE;
		int expectedLineWidth = 1;
		assertEquals(expectedLineWidth, expectedLineWidth);
		assertEquals(expectedColor, actualColor);
	}
	@Test
	public void testBarChartCommonConfigurationExtraction() {
		Image imageSampleLineChart = new Image(
				"src/main/resources/TestCharts/BarCharts/Uncut/host_mem_capacity.png");
		ChartCropper cropper = new ChartCropper(imageSampleLineChart);
		ChartConfigurator commonConfig=new ChartConfigurator(cropper.cropAxises());
		Color actualColor = commonConfig.getBackgroundColor();
		int actualLineWidth = commonConfig.getMainLineWidth();
		Color expectedColor = StandardColors.WHITE;
		int expectedLineWidth = 1;
		assertEquals(expectedLineWidth, expectedLineWidth);
		assertEquals(expectedColor, actualColor);
	}
*/
}
